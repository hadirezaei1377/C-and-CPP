main is the first thing for complier!

 #include <iostream>  // preprocessing command
 using namespace std; // preprocessing command

int main()
{ /* start commands
	cout << "hello world\n";
	system("pause");
	return 0;
} */ finish commands

declaring variables: 
                   1-with initialization:
                   int i = 0;
                   2-with assignment:
                   int i;
                   i = 0;
                   i = 1;
                   .
                   .
                   .
                   3-with input:
                   int i;
                   cin>>i;

declaring constants: 
                   1-with #define + name + value  -------> no need to ; because its not command and its preprocessing command (before strarting compiler)
                   #define A 1
                   #define H "Hadi"
                   2-const + type + name = value
                   const int i=0; // i is defualt 


type conversion :
                1-automatic : 
                do by compliler 
                for example : int/int ----> double
                2-explicit :
                do by programmer
                type cast
                

    